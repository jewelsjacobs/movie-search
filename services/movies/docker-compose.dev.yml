version: "2.1"
services:
  movies-service:
    command: yarn dev -- -L
    environment:
      - MONGO_URI=mongodb://mongodb:27017
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - "27017:27017"
