version: "2.1"
services:
  movies-service:
    command: yarn test
    environment:
      - MONGO_URI_TESTS=mongodb://mongodb:27017
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - "27017:27017"
